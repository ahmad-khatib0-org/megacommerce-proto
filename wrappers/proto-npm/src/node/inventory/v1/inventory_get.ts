// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.7
//   protoc               unknown
// source: inventory/v1/inventory_get.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { AppError } from "../../shared/v1/error";
import { Struct } from "../../shared/v1/struct";

export const protobufPackage = "inventory.v1";

/** Request for getting inventory */
export interface InventoryGetRequest {
  productIds: string[];
  variantIds: string[];
  skus: string[];
}

export interface InventoryGetResponse {
  data?: InventoryGetResponseData | undefined;
  error?: AppError | undefined;
}

export interface InventoryGetResponseData {
  items: InventoryItemList[];
}

export interface InventoryItemList {
  id: string;
  productId: string;
  variantId: string;
  sku: string;
  quantityTotal: number;
  quantityReserved: number;
  quantityAvailable: number;
  /** Optional */
  locationId: string;
  metadata?: Struct | undefined;
}

function createBaseInventoryGetRequest(): InventoryGetRequest {
  return { productIds: [], variantIds: [], skus: [] };
}

export const InventoryGetRequest: MessageFns<InventoryGetRequest> = {
  encode(message: InventoryGetRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.productIds) {
      writer.uint32(10).string(v!);
    }
    for (const v of message.variantIds) {
      writer.uint32(18).string(v!);
    }
    for (const v of message.skus) {
      writer.uint32(26).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): InventoryGetRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseInventoryGetRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.productIds.push(reader.string());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.variantIds.push(reader.string());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.skus.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): InventoryGetRequest {
    return {
      productIds: globalThis.Array.isArray(object?.productIds)
        ? object.productIds.map((e: any) => globalThis.String(e))
        : [],
      variantIds: globalThis.Array.isArray(object?.variantIds)
        ? object.variantIds.map((e: any) => globalThis.String(e))
        : [],
      skus: globalThis.Array.isArray(object?.skus) ? object.skus.map((e: any) => globalThis.String(e)) : [],
    };
  },

  toJSON(message: InventoryGetRequest): unknown {
    const obj: any = {};
    if (message.productIds?.length) {
      obj.productIds = message.productIds;
    }
    if (message.variantIds?.length) {
      obj.variantIds = message.variantIds;
    }
    if (message.skus?.length) {
      obj.skus = message.skus;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<InventoryGetRequest>, I>>(base?: I): InventoryGetRequest {
    return InventoryGetRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<InventoryGetRequest>, I>>(object: I): InventoryGetRequest {
    const message = createBaseInventoryGetRequest();
    message.productIds = object.productIds?.map((e) => e) || [];
    message.variantIds = object.variantIds?.map((e) => e) || [];
    message.skus = object.skus?.map((e) => e) || [];
    return message;
  },
};

function createBaseInventoryGetResponse(): InventoryGetResponse {
  return { data: undefined, error: undefined };
}

export const InventoryGetResponse: MessageFns<InventoryGetResponse> = {
  encode(message: InventoryGetResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.data !== undefined) {
      InventoryGetResponseData.encode(message.data, writer.uint32(10).fork()).join();
    }
    if (message.error !== undefined) {
      AppError.encode(message.error, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): InventoryGetResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseInventoryGetResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.data = InventoryGetResponseData.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.error = AppError.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): InventoryGetResponse {
    return {
      data: isSet(object.data) ? InventoryGetResponseData.fromJSON(object.data) : undefined,
      error: isSet(object.error) ? AppError.fromJSON(object.error) : undefined,
    };
  },

  toJSON(message: InventoryGetResponse): unknown {
    const obj: any = {};
    if (message.data !== undefined) {
      obj.data = InventoryGetResponseData.toJSON(message.data);
    }
    if (message.error !== undefined) {
      obj.error = AppError.toJSON(message.error);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<InventoryGetResponse>, I>>(base?: I): InventoryGetResponse {
    return InventoryGetResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<InventoryGetResponse>, I>>(object: I): InventoryGetResponse {
    const message = createBaseInventoryGetResponse();
    message.data = (object.data !== undefined && object.data !== null)
      ? InventoryGetResponseData.fromPartial(object.data)
      : undefined;
    message.error = (object.error !== undefined && object.error !== null)
      ? AppError.fromPartial(object.error)
      : undefined;
    return message;
  },
};

function createBaseInventoryGetResponseData(): InventoryGetResponseData {
  return { items: [] };
}

export const InventoryGetResponseData: MessageFns<InventoryGetResponseData> = {
  encode(message: InventoryGetResponseData, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.items) {
      InventoryItemList.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): InventoryGetResponseData {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseInventoryGetResponseData();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.items.push(InventoryItemList.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): InventoryGetResponseData {
    return {
      items: globalThis.Array.isArray(object?.items) ? object.items.map((e: any) => InventoryItemList.fromJSON(e)) : [],
    };
  },

  toJSON(message: InventoryGetResponseData): unknown {
    const obj: any = {};
    if (message.items?.length) {
      obj.items = message.items.map((e) => InventoryItemList.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<InventoryGetResponseData>, I>>(base?: I): InventoryGetResponseData {
    return InventoryGetResponseData.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<InventoryGetResponseData>, I>>(object: I): InventoryGetResponseData {
    const message = createBaseInventoryGetResponseData();
    message.items = object.items?.map((e) => InventoryItemList.fromPartial(e)) || [];
    return message;
  },
};

function createBaseInventoryItemList(): InventoryItemList {
  return {
    id: "",
    productId: "",
    variantId: "",
    sku: "",
    quantityTotal: 0,
    quantityReserved: 0,
    quantityAvailable: 0,
    locationId: "",
    metadata: undefined,
  };
}

export const InventoryItemList: MessageFns<InventoryItemList> = {
  encode(message: InventoryItemList, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.productId !== "") {
      writer.uint32(18).string(message.productId);
    }
    if (message.variantId !== "") {
      writer.uint32(26).string(message.variantId);
    }
    if (message.sku !== "") {
      writer.uint32(34).string(message.sku);
    }
    if (message.quantityTotal !== 0) {
      writer.uint32(40).uint32(message.quantityTotal);
    }
    if (message.quantityReserved !== 0) {
      writer.uint32(48).uint32(message.quantityReserved);
    }
    if (message.quantityAvailable !== 0) {
      writer.uint32(56).uint32(message.quantityAvailable);
    }
    if (message.locationId !== "") {
      writer.uint32(66).string(message.locationId);
    }
    if (message.metadata !== undefined) {
      Struct.encode(message.metadata, writer.uint32(74).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): InventoryItemList {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseInventoryItemList();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.productId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.variantId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.sku = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.quantityTotal = reader.uint32();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.quantityReserved = reader.uint32();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.quantityAvailable = reader.uint32();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.locationId = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.metadata = Struct.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): InventoryItemList {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      productId: isSet(object.productId) ? globalThis.String(object.productId) : "",
      variantId: isSet(object.variantId) ? globalThis.String(object.variantId) : "",
      sku: isSet(object.sku) ? globalThis.String(object.sku) : "",
      quantityTotal: isSet(object.quantityTotal) ? globalThis.Number(object.quantityTotal) : 0,
      quantityReserved: isSet(object.quantityReserved) ? globalThis.Number(object.quantityReserved) : 0,
      quantityAvailable: isSet(object.quantityAvailable) ? globalThis.Number(object.quantityAvailable) : 0,
      locationId: isSet(object.locationId) ? globalThis.String(object.locationId) : "",
      metadata: isSet(object.metadata) ? Struct.fromJSON(object.metadata) : undefined,
    };
  },

  toJSON(message: InventoryItemList): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.productId !== "") {
      obj.productId = message.productId;
    }
    if (message.variantId !== "") {
      obj.variantId = message.variantId;
    }
    if (message.sku !== "") {
      obj.sku = message.sku;
    }
    if (message.quantityTotal !== 0) {
      obj.quantityTotal = Math.round(message.quantityTotal);
    }
    if (message.quantityReserved !== 0) {
      obj.quantityReserved = Math.round(message.quantityReserved);
    }
    if (message.quantityAvailable !== 0) {
      obj.quantityAvailable = Math.round(message.quantityAvailable);
    }
    if (message.locationId !== "") {
      obj.locationId = message.locationId;
    }
    if (message.metadata !== undefined) {
      obj.metadata = Struct.toJSON(message.metadata);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<InventoryItemList>, I>>(base?: I): InventoryItemList {
    return InventoryItemList.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<InventoryItemList>, I>>(object: I): InventoryItemList {
    const message = createBaseInventoryItemList();
    message.id = object.id ?? "";
    message.productId = object.productId ?? "";
    message.variantId = object.variantId ?? "";
    message.sku = object.sku ?? "";
    message.quantityTotal = object.quantityTotal ?? 0;
    message.quantityReserved = object.quantityReserved ?? 0;
    message.quantityAvailable = object.quantityAvailable ?? 0;
    message.locationId = object.locationId ?? "";
    message.metadata = (object.metadata !== undefined && object.metadata !== null)
      ? Struct.fromPartial(object.metadata)
      : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
