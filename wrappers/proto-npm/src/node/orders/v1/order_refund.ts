// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.7
//   protoc               unknown
// source: orders/v1/order_refund.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { AppError } from "../../shared/v1/error";
import { SuccessResponseData } from "../../shared/v1/types";

export const protobufPackage = "orders.v1";

export interface OrderRefundResponse {
  data?: SuccessResponseData | undefined;
  error?: AppError | undefined;
}

export interface OrderRefundRequest {
  orderId: string;
  /** refund whole order or specific line items */
  lineItems?:
    | RefundLineItemRefund
    | undefined;
  /** reason code, external refund id, etc. */
  reason: string;
  refundShipping: boolean;
}

export interface RefundLineItemRefund {
  /** link to OrderLineItem.id */
  id: string;
  quantity: number;
  /**
   * amount to refund in minor units (cents). If zero, compute pro-rata.
   *
   * **Pro-rata refund** = Refund proportional to the original price distribution.
   *
   * **Example:**
   * - Order total: $100 ($60 Item A + $40 Item B)
   * - Shipping: $10
   * - You refund Item B ($40)
   * - Pro-rata calculation: Item B was 40% of product total ($40/$100)
   * - Refund: $40 (item) + $4 (40% of shipping) = **$44 total refund**
   *
   * **Without pro-rata:** You'd only refund $40 (lose shipping cost)
   *
   * **Used when:** Partial refunds where shipping/taxes/fees need to be fairly distributed.
   */
  amountCents?: string | undefined;
}

function createBaseOrderRefundResponse(): OrderRefundResponse {
  return { data: undefined, error: undefined };
}

export const OrderRefundResponse: MessageFns<OrderRefundResponse> = {
  encode(message: OrderRefundResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.data !== undefined) {
      SuccessResponseData.encode(message.data, writer.uint32(10).fork()).join();
    }
    if (message.error !== undefined) {
      AppError.encode(message.error, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): OrderRefundResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseOrderRefundResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.data = SuccessResponseData.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.error = AppError.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): OrderRefundResponse {
    return {
      data: isSet(object.data) ? SuccessResponseData.fromJSON(object.data) : undefined,
      error: isSet(object.error) ? AppError.fromJSON(object.error) : undefined,
    };
  },

  toJSON(message: OrderRefundResponse): unknown {
    const obj: any = {};
    if (message.data !== undefined) {
      obj.data = SuccessResponseData.toJSON(message.data);
    }
    if (message.error !== undefined) {
      obj.error = AppError.toJSON(message.error);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<OrderRefundResponse>, I>>(base?: I): OrderRefundResponse {
    return OrderRefundResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<OrderRefundResponse>, I>>(object: I): OrderRefundResponse {
    const message = createBaseOrderRefundResponse();
    message.data = (object.data !== undefined && object.data !== null)
      ? SuccessResponseData.fromPartial(object.data)
      : undefined;
    message.error = (object.error !== undefined && object.error !== null)
      ? AppError.fromPartial(object.error)
      : undefined;
    return message;
  },
};

function createBaseOrderRefundRequest(): OrderRefundRequest {
  return { orderId: "", lineItems: undefined, reason: "", refundShipping: false };
}

export const OrderRefundRequest: MessageFns<OrderRefundRequest> = {
  encode(message: OrderRefundRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.orderId !== "") {
      writer.uint32(10).string(message.orderId);
    }
    if (message.lineItems !== undefined) {
      RefundLineItemRefund.encode(message.lineItems, writer.uint32(18).fork()).join();
    }
    if (message.reason !== "") {
      writer.uint32(26).string(message.reason);
    }
    if (message.refundShipping !== false) {
      writer.uint32(32).bool(message.refundShipping);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): OrderRefundRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseOrderRefundRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.orderId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.lineItems = RefundLineItemRefund.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.reason = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.refundShipping = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): OrderRefundRequest {
    return {
      orderId: isSet(object.orderId) ? globalThis.String(object.orderId) : "",
      lineItems: isSet(object.lineItems) ? RefundLineItemRefund.fromJSON(object.lineItems) : undefined,
      reason: isSet(object.reason) ? globalThis.String(object.reason) : "",
      refundShipping: isSet(object.refundShipping) ? globalThis.Boolean(object.refundShipping) : false,
    };
  },

  toJSON(message: OrderRefundRequest): unknown {
    const obj: any = {};
    if (message.orderId !== "") {
      obj.orderId = message.orderId;
    }
    if (message.lineItems !== undefined) {
      obj.lineItems = RefundLineItemRefund.toJSON(message.lineItems);
    }
    if (message.reason !== "") {
      obj.reason = message.reason;
    }
    if (message.refundShipping !== false) {
      obj.refundShipping = message.refundShipping;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<OrderRefundRequest>, I>>(base?: I): OrderRefundRequest {
    return OrderRefundRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<OrderRefundRequest>, I>>(object: I): OrderRefundRequest {
    const message = createBaseOrderRefundRequest();
    message.orderId = object.orderId ?? "";
    message.lineItems = (object.lineItems !== undefined && object.lineItems !== null)
      ? RefundLineItemRefund.fromPartial(object.lineItems)
      : undefined;
    message.reason = object.reason ?? "";
    message.refundShipping = object.refundShipping ?? false;
    return message;
  },
};

function createBaseRefundLineItemRefund(): RefundLineItemRefund {
  return { id: "", quantity: 0, amountCents: undefined };
}

export const RefundLineItemRefund: MessageFns<RefundLineItemRefund> = {
  encode(message: RefundLineItemRefund, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.quantity !== 0) {
      writer.uint32(16).uint32(message.quantity);
    }
    if (message.amountCents !== undefined) {
      writer.uint32(24).uint64(message.amountCents);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RefundLineItemRefund {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRefundLineItemRefund();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.quantity = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.amountCents = reader.uint64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RefundLineItemRefund {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      quantity: isSet(object.quantity) ? globalThis.Number(object.quantity) : 0,
      amountCents: isSet(object.amountCents) ? globalThis.String(object.amountCents) : undefined,
    };
  },

  toJSON(message: RefundLineItemRefund): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.quantity !== 0) {
      obj.quantity = Math.round(message.quantity);
    }
    if (message.amountCents !== undefined) {
      obj.amountCents = message.amountCents;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RefundLineItemRefund>, I>>(base?: I): RefundLineItemRefund {
    return RefundLineItemRefund.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RefundLineItemRefund>, I>>(object: I): RefundLineItemRefund {
    const message = createBaseRefundLineItemRefund();
    message.id = object.id ?? "";
    message.quantity = object.quantity ?? 0;
    message.amountCents = object.amountCents ?? undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
