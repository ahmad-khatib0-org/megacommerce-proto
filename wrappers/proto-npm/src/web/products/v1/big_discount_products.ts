// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.7
//   protoc               unknown
// source: products/v1/big_discount_products.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { AppError } from "../../shared/v1/error.js";

export const protobufPackage = "products.v1";

export interface BigDiscountProductsRequest {
}

export interface BigDiscountProductsResponse {
  data?: BigDiscountProductsResponseData | undefined;
  error?: AppError | undefined;
}

export interface BigDiscountProductsResponseData {
  products: BigDiscountProductListItem[];
}

export interface BigDiscountProductListItem {
  id: string;
  title: string;
  image: string;
  priceCents: number;
  discountPriceCents: number;
  discountPercentage: number;
  soldCount: number;
  variantId: string;
}

function createBaseBigDiscountProductsRequest(): BigDiscountProductsRequest {
  return {};
}

export const BigDiscountProductsRequest: MessageFns<BigDiscountProductsRequest> = {
  encode(_: BigDiscountProductsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BigDiscountProductsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBigDiscountProductsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): BigDiscountProductsRequest {
    return {};
  },

  toJSON(_: BigDiscountProductsRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<BigDiscountProductsRequest>, I>>(base?: I): BigDiscountProductsRequest {
    return BigDiscountProductsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BigDiscountProductsRequest>, I>>(_: I): BigDiscountProductsRequest {
    const message = createBaseBigDiscountProductsRequest();
    return message;
  },
};

function createBaseBigDiscountProductsResponse(): BigDiscountProductsResponse {
  return { data: undefined, error: undefined };
}

export const BigDiscountProductsResponse: MessageFns<BigDiscountProductsResponse> = {
  encode(message: BigDiscountProductsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.data !== undefined) {
      BigDiscountProductsResponseData.encode(message.data, writer.uint32(10).fork()).join();
    }
    if (message.error !== undefined) {
      AppError.encode(message.error, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BigDiscountProductsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBigDiscountProductsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.data = BigDiscountProductsResponseData.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.error = AppError.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BigDiscountProductsResponse {
    return {
      data: isSet(object.data) ? BigDiscountProductsResponseData.fromJSON(object.data) : undefined,
      error: isSet(object.error) ? AppError.fromJSON(object.error) : undefined,
    };
  },

  toJSON(message: BigDiscountProductsResponse): unknown {
    const obj: any = {};
    if (message.data !== undefined) {
      obj.data = BigDiscountProductsResponseData.toJSON(message.data);
    }
    if (message.error !== undefined) {
      obj.error = AppError.toJSON(message.error);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BigDiscountProductsResponse>, I>>(base?: I): BigDiscountProductsResponse {
    return BigDiscountProductsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BigDiscountProductsResponse>, I>>(object: I): BigDiscountProductsResponse {
    const message = createBaseBigDiscountProductsResponse();
    message.data = (object.data !== undefined && object.data !== null)
      ? BigDiscountProductsResponseData.fromPartial(object.data)
      : undefined;
    message.error = (object.error !== undefined && object.error !== null)
      ? AppError.fromPartial(object.error)
      : undefined;
    return message;
  },
};

function createBaseBigDiscountProductsResponseData(): BigDiscountProductsResponseData {
  return { products: [] };
}

export const BigDiscountProductsResponseData: MessageFns<BigDiscountProductsResponseData> = {
  encode(message: BigDiscountProductsResponseData, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.products) {
      BigDiscountProductListItem.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BigDiscountProductsResponseData {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBigDiscountProductsResponseData();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.products.push(BigDiscountProductListItem.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BigDiscountProductsResponseData {
    return {
      products: globalThis.Array.isArray(object?.products)
        ? object.products.map((e: any) => BigDiscountProductListItem.fromJSON(e))
        : [],
    };
  },

  toJSON(message: BigDiscountProductsResponseData): unknown {
    const obj: any = {};
    if (message.products?.length) {
      obj.products = message.products.map((e) => BigDiscountProductListItem.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BigDiscountProductsResponseData>, I>>(base?: I): BigDiscountProductsResponseData {
    return BigDiscountProductsResponseData.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BigDiscountProductsResponseData>, I>>(
    object: I,
  ): BigDiscountProductsResponseData {
    const message = createBaseBigDiscountProductsResponseData();
    message.products = object.products?.map((e) => BigDiscountProductListItem.fromPartial(e)) || [];
    return message;
  },
};

function createBaseBigDiscountProductListItem(): BigDiscountProductListItem {
  return {
    id: "",
    title: "",
    image: "",
    priceCents: 0,
    discountPriceCents: 0,
    discountPercentage: 0,
    soldCount: 0,
    variantId: "",
  };
}

export const BigDiscountProductListItem: MessageFns<BigDiscountProductListItem> = {
  encode(message: BigDiscountProductListItem, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.title !== "") {
      writer.uint32(18).string(message.title);
    }
    if (message.image !== "") {
      writer.uint32(26).string(message.image);
    }
    if (message.priceCents !== 0) {
      writer.uint32(32).uint32(message.priceCents);
    }
    if (message.discountPriceCents !== 0) {
      writer.uint32(40).uint32(message.discountPriceCents);
    }
    if (message.discountPercentage !== 0) {
      writer.uint32(48).uint32(message.discountPercentage);
    }
    if (message.soldCount !== 0) {
      writer.uint32(56).uint32(message.soldCount);
    }
    if (message.variantId !== "") {
      writer.uint32(66).string(message.variantId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BigDiscountProductListItem {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBigDiscountProductListItem();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.title = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.image = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.priceCents = reader.uint32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.discountPriceCents = reader.uint32();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.discountPercentage = reader.uint32();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.soldCount = reader.uint32();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.variantId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BigDiscountProductListItem {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      title: isSet(object.title) ? globalThis.String(object.title) : "",
      image: isSet(object.image) ? globalThis.String(object.image) : "",
      priceCents: isSet(object.priceCents) ? globalThis.Number(object.priceCents) : 0,
      discountPriceCents: isSet(object.discountPriceCents) ? globalThis.Number(object.discountPriceCents) : 0,
      discountPercentage: isSet(object.discountPercentage) ? globalThis.Number(object.discountPercentage) : 0,
      soldCount: isSet(object.soldCount) ? globalThis.Number(object.soldCount) : 0,
      variantId: isSet(object.variantId) ? globalThis.String(object.variantId) : "",
    };
  },

  toJSON(message: BigDiscountProductListItem): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.title !== "") {
      obj.title = message.title;
    }
    if (message.image !== "") {
      obj.image = message.image;
    }
    if (message.priceCents !== 0) {
      obj.priceCents = Math.round(message.priceCents);
    }
    if (message.discountPriceCents !== 0) {
      obj.discountPriceCents = Math.round(message.discountPriceCents);
    }
    if (message.discountPercentage !== 0) {
      obj.discountPercentage = Math.round(message.discountPercentage);
    }
    if (message.soldCount !== 0) {
      obj.soldCount = Math.round(message.soldCount);
    }
    if (message.variantId !== "") {
      obj.variantId = message.variantId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BigDiscountProductListItem>, I>>(base?: I): BigDiscountProductListItem {
    return BigDiscountProductListItem.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BigDiscountProductListItem>, I>>(object: I): BigDiscountProductListItem {
    const message = createBaseBigDiscountProductListItem();
    message.id = object.id ?? "";
    message.title = object.title ?? "";
    message.image = object.image ?? "";
    message.priceCents = object.priceCents ?? 0;
    message.discountPriceCents = object.discountPriceCents ?? 0;
    message.discountPercentage = object.discountPercentage ?? 0;
    message.soldCount = object.soldCount ?? 0;
    message.variantId = object.variantId ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
