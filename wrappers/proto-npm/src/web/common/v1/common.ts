// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.7
//   protoc               unknown
// source: common/v1/common.proto

/* eslint-disable */
import { grpc } from "@improbable-eng/grpc-web";
import { BrowserHeaders } from "browser-headers";
import { Observable } from "rxjs";
import { share } from "rxjs/operators";
import { PingRequest, PingResponse } from "../../shared/v1/types.js";
import {
  ConfigGetRequest,
  ConfigGetResponse,
  ConfigListenerRequest,
  ConfigListenerResponse,
  ConfigUpdateRequest,
  ConfigUpdateResponse,
} from "./config.js";
import {
  TranslationsForLangGetRequest,
  TranslationsForLangGetResponse,
  TranslationsGetRequest,
  TranslationsGetResponse,
} from "./trans.js";

export const protobufPackage = "common.v1";

export interface CommonService {
  Ping(request: DeepPartial<PingRequest>, metadata?: grpc.Metadata): Promise<PingResponse>;
  ConfigGet(request: DeepPartial<ConfigGetRequest>, metadata?: grpc.Metadata): Promise<ConfigGetResponse>;
  ConfigUpdate(request: DeepPartial<ConfigUpdateRequest>, metadata?: grpc.Metadata): Promise<ConfigUpdateResponse>;
  ConfigListener(
    request: DeepPartial<ConfigListenerRequest>,
    metadata?: grpc.Metadata,
  ): Observable<ConfigListenerResponse>;
  TranslationsGet(
    request: DeepPartial<TranslationsGetRequest>,
    metadata?: grpc.Metadata,
  ): Promise<TranslationsGetResponse>;
  TranslationsForLangGet(
    request: DeepPartial<TranslationsForLangGetRequest>,
    metadata?: grpc.Metadata,
  ): Promise<TranslationsForLangGetResponse>;
}

export class CommonServiceClientImpl implements CommonService {
  private readonly rpc: Rpc;

  constructor(rpc: Rpc) {
    this.rpc = rpc;
    this.Ping = this.Ping.bind(this);
    this.ConfigGet = this.ConfigGet.bind(this);
    this.ConfigUpdate = this.ConfigUpdate.bind(this);
    this.ConfigListener = this.ConfigListener.bind(this);
    this.TranslationsGet = this.TranslationsGet.bind(this);
    this.TranslationsForLangGet = this.TranslationsForLangGet.bind(this);
  }

  Ping(request: DeepPartial<PingRequest>, metadata?: grpc.Metadata): Promise<PingResponse> {
    return this.rpc.unary(CommonServicePingDesc, PingRequest.fromPartial(request), metadata);
  }

  ConfigGet(request: DeepPartial<ConfigGetRequest>, metadata?: grpc.Metadata): Promise<ConfigGetResponse> {
    return this.rpc.unary(CommonServiceConfigGetDesc, ConfigGetRequest.fromPartial(request), metadata);
  }

  ConfigUpdate(request: DeepPartial<ConfigUpdateRequest>, metadata?: grpc.Metadata): Promise<ConfigUpdateResponse> {
    return this.rpc.unary(CommonServiceConfigUpdateDesc, ConfigUpdateRequest.fromPartial(request), metadata);
  }

  ConfigListener(
    request: DeepPartial<ConfigListenerRequest>,
    metadata?: grpc.Metadata,
  ): Observable<ConfigListenerResponse> {
    return this.rpc.invoke(CommonServiceConfigListenerDesc, ConfigListenerRequest.fromPartial(request), metadata);
  }

  TranslationsGet(
    request: DeepPartial<TranslationsGetRequest>,
    metadata?: grpc.Metadata,
  ): Promise<TranslationsGetResponse> {
    return this.rpc.unary(CommonServiceTranslationsGetDesc, TranslationsGetRequest.fromPartial(request), metadata);
  }

  TranslationsForLangGet(
    request: DeepPartial<TranslationsForLangGetRequest>,
    metadata?: grpc.Metadata,
  ): Promise<TranslationsForLangGetResponse> {
    return this.rpc.unary(
      CommonServiceTranslationsForLangGetDesc,
      TranslationsForLangGetRequest.fromPartial(request),
      metadata,
    );
  }
}

export const CommonServiceDesc = { serviceName: "common.v1.CommonService" };

export const CommonServicePingDesc: UnaryMethodDefinitionish = {
  methodName: "Ping",
  service: CommonServiceDesc,
  requestStream: false,
  responseStream: false,
  requestType: {
    serializeBinary() {
      return PingRequest.encode(this).finish();
    },
  } as any,
  responseType: {
    deserializeBinary(data: Uint8Array) {
      const value = PingResponse.decode(data);
      return {
        ...value,
        toObject() {
          return value;
        },
      };
    },
  } as any,
};

export const CommonServiceConfigGetDesc: UnaryMethodDefinitionish = {
  methodName: "ConfigGet",
  service: CommonServiceDesc,
  requestStream: false,
  responseStream: false,
  requestType: {
    serializeBinary() {
      return ConfigGetRequest.encode(this).finish();
    },
  } as any,
  responseType: {
    deserializeBinary(data: Uint8Array) {
      const value = ConfigGetResponse.decode(data);
      return {
        ...value,
        toObject() {
          return value;
        },
      };
    },
  } as any,
};

export const CommonServiceConfigUpdateDesc: UnaryMethodDefinitionish = {
  methodName: "ConfigUpdate",
  service: CommonServiceDesc,
  requestStream: false,
  responseStream: false,
  requestType: {
    serializeBinary() {
      return ConfigUpdateRequest.encode(this).finish();
    },
  } as any,
  responseType: {
    deserializeBinary(data: Uint8Array) {
      const value = ConfigUpdateResponse.decode(data);
      return {
        ...value,
        toObject() {
          return value;
        },
      };
    },
  } as any,
};

export const CommonServiceConfigListenerDesc: UnaryMethodDefinitionish = {
  methodName: "ConfigListener",
  service: CommonServiceDesc,
  requestStream: false,
  responseStream: true,
  requestType: {
    serializeBinary() {
      return ConfigListenerRequest.encode(this).finish();
    },
  } as any,
  responseType: {
    deserializeBinary(data: Uint8Array) {
      const value = ConfigListenerResponse.decode(data);
      return {
        ...value,
        toObject() {
          return value;
        },
      };
    },
  } as any,
};

export const CommonServiceTranslationsGetDesc: UnaryMethodDefinitionish = {
  methodName: "TranslationsGet",
  service: CommonServiceDesc,
  requestStream: false,
  responseStream: false,
  requestType: {
    serializeBinary() {
      return TranslationsGetRequest.encode(this).finish();
    },
  } as any,
  responseType: {
    deserializeBinary(data: Uint8Array) {
      const value = TranslationsGetResponse.decode(data);
      return {
        ...value,
        toObject() {
          return value;
        },
      };
    },
  } as any,
};

export const CommonServiceTranslationsForLangGetDesc: UnaryMethodDefinitionish = {
  methodName: "TranslationsForLangGet",
  service: CommonServiceDesc,
  requestStream: false,
  responseStream: false,
  requestType: {
    serializeBinary() {
      return TranslationsForLangGetRequest.encode(this).finish();
    },
  } as any,
  responseType: {
    deserializeBinary(data: Uint8Array) {
      const value = TranslationsForLangGetResponse.decode(data);
      return {
        ...value,
        toObject() {
          return value;
        },
      };
    },
  } as any,
};

interface UnaryMethodDefinitionishR extends grpc.UnaryMethodDefinition<any, any> {
  requestStream: any;
  responseStream: any;
}

type UnaryMethodDefinitionish = UnaryMethodDefinitionishR;

interface Rpc {
  unary<T extends UnaryMethodDefinitionish>(
    methodDesc: T,
    request: any,
    metadata: grpc.Metadata | undefined,
  ): Promise<any>;
  invoke<T extends UnaryMethodDefinitionish>(
    methodDesc: T,
    request: any,
    metadata: grpc.Metadata | undefined,
  ): Observable<any>;
}

export class GrpcWebImpl {
  private host: string;
  private options: {
    transport?: grpc.TransportFactory;
    streamingTransport?: grpc.TransportFactory;
    debug?: boolean;
    metadata?: grpc.Metadata;
    upStreamRetryCodes?: number[];
  };

  constructor(
    host: string,
    options: {
      transport?: grpc.TransportFactory;
      streamingTransport?: grpc.TransportFactory;
      debug?: boolean;
      metadata?: grpc.Metadata;
      upStreamRetryCodes?: number[];
    },
  ) {
    this.host = host;
    this.options = options;
  }

  unary<T extends UnaryMethodDefinitionish>(
    methodDesc: T,
    _request: any,
    metadata: grpc.Metadata | undefined,
  ): Promise<any> {
    const request = { ..._request, ...methodDesc.requestType };
    const maybeCombinedMetadata = metadata && this.options.metadata
      ? new BrowserHeaders({ ...this.options?.metadata.headersMap, ...metadata?.headersMap })
      : metadata ?? this.options.metadata;
    return new Promise((resolve, reject) => {
      grpc.unary(methodDesc, {
        request,
        host: this.host,
        metadata: maybeCombinedMetadata ?? {},
        ...(this.options.transport !== undefined ? { transport: this.options.transport } : {}),
        debug: this.options.debug ?? false,
        onEnd: function (response) {
          if (response.status === grpc.Code.OK) {
            resolve(response.message!.toObject());
          } else {
            const err = new GrpcWebError(response.statusMessage, response.status, response.trailers);
            reject(err);
          }
        },
      });
    });
  }

  invoke<T extends UnaryMethodDefinitionish>(
    methodDesc: T,
    _request: any,
    metadata: grpc.Metadata | undefined,
  ): Observable<any> {
    const upStreamCodes = this.options.upStreamRetryCodes ?? [];
    const DEFAULT_TIMEOUT_TIME: number = 3_000;
    const request = { ..._request, ...methodDesc.requestType };
    const transport = this.options.streamingTransport ?? this.options.transport;
    const maybeCombinedMetadata = metadata && this.options.metadata
      ? new BrowserHeaders({ ...this.options?.metadata.headersMap, ...metadata?.headersMap })
      : metadata ?? this.options.metadata;
    return new Observable((observer) => {
      const upStream = () => {
        const client = grpc.invoke(methodDesc, {
          host: this.host,
          request,
          ...(transport !== undefined ? { transport } : {}),
          metadata: maybeCombinedMetadata ?? {},
          debug: this.options.debug ?? false,
          onMessage: (next) => observer.next(next),
          onEnd: (code: grpc.Code, message: string, trailers: grpc.Metadata) => {
            if (code === 0) {
              observer.complete();
            } else if (upStreamCodes.includes(code)) {
              setTimeout(upStream, DEFAULT_TIMEOUT_TIME);
            } else {
              const err = new Error(message) as any;
              err.code = code;
              err.metadata = trailers;
              observer.error(err);
            }
          },
        });
        observer.add(() => client.close());
      };
      upStream();
    }).pipe(share());
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

export class GrpcWebError extends globalThis.Error {
  constructor(message: string, public code: grpc.Code, public metadata: grpc.Metadata) {
    super(message);
  }
}
